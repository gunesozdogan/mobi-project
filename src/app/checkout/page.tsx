'use client';

import CartItemContainer from '@/app/components/CartItemContainer';

const CheckoutPage = () => {
  return (
    <div className="bg-bg-primary dark:bg-dark-bg-primary w-full min-h-screen">
      <CartItemContainer />
    </div>
  );
};

export default CheckoutPage;
